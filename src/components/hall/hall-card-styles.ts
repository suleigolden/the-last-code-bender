/** Shared layout so claimed and unclaimed hall cards match height in the grid. */
export const HALL_CARD_CLASS = 'h-full min-h-[152px]';
export const HALL_CARD_CONTENT_CLASS = 'p-4 flex flex-col gap-3 h-full';
